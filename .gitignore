# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
.next/

# Env
.env

# OS
.DS_Store

# Personal study notes — local only
practice/

# Raw verification run log — regenerated by tests/verify_pipeline.py on every
# run, and its findings belong in CONTEXT.md rather than in the tree.
backend/tests/_verify_run.log

# Local Claude Code settings (may contain machine-specific values)
.claude/settings.local.json
