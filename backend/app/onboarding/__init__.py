"""Owner-account onboarding (deploy gap #1, PLAN_owner_onboarding.md).

The platform-mediated flows that connect an OWNER's real provider accounts before
deploy — starting with Stripe Connect (click-to-connect). Kept separate from the
DevOps deploy path (`app/devops`) and the BA conversation (`app/ba`); those call in.
"""
